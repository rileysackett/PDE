# PDE
This repo contains a lightweight PDE solver framework (1D examples) implemented in C++.
First milestone: Crank–Nicolson 1D heat equation solver with Thomas tridiagonal solver and manufactured-solution verification.

## Build
```bash
cd src
make
